name: Structural Tests
on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Run coverage tool
        uses: ./.github/actions/blackduck-scan-action
        with:
          token: ${{ secrets.FABRIC_BLACKDUCK_SCAN_TOKEN }}
          blackduck-log: "fsdfmsfmsmfklsfmskld lksdfskfsklc slkdfjskflskl sldkf"
